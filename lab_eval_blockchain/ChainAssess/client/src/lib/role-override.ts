// Temporary role override for immediate testing
// DISABLED: Role override removed to show actual blockchain roles

export function getOverrideRole(address: string): string | null {
  // Role override disabled - now showing real blockchain roles
  // This allows the Register Teacher button to appear for admin users
  return null;
}